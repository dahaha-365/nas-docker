services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - '${PORT:-6379}:6379'
    volumes:
      - ./data:/data
      - ./redis.conf:/usr/local/etc/redis/redis.conf
      - ./logs:/logs
